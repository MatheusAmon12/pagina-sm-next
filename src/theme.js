import { createTheme } from "@mui/material"


const theme = createTheme({
    palette: {
        primary: {
            main: "#4E0041",
        },
        secondary: {
            main: "#070707",
        },
        background: {
            default: '#F6F8FF'
        }
    },
    
})

export default theme